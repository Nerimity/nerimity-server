export enum ChannelType {
  DM_TEXT = 0,
  SERVER_TEXT = 1,
  CATEGORY = 2,
  TICKET = 3,
}

export const TextChannelTypes = [
  ChannelType.DM_TEXT,
  ChannelType.SERVER_TEXT,
  ChannelType.TICKET,
];
