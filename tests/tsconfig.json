{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "types": ["node", "vitest/globals"]
    // Remove typeRoots for a moment to see if it resolves
  },
  "include": [
    "./**/*.ts", 
    "../src/**/*.ts" 
  ]
}